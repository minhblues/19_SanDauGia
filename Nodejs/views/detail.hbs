{{#section 'js'}}
<script src="/javascripts/bootstrap-input-spinner.js"></script>
<script>
    if({{product.StepPrice}})
        $("input[type='number']").prop('min',parseInt($("input[type='number']").prop('min'))+{{product.StepPrice}});
    $("input[type='number']").inputSpinner();
    $.ajax({
        type: "post",
        url: "/detail/auction/{{ID}}",
        success: (data,text)=>alert("Đấu giá thành công")
    })
</script>
{{/section}}
{{>header}}
<!-- breadcrumb start-->
<section class="breadcrumb breadcrumb_bg">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-sm-12">
                <div class="breadcrumb_iner">
                    <div class="breadcrumb_iner_item">
                        <p>Trang chủ/ Điện tử / Điện thoại di động</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
<!-- breadcrumb start-->
<!--================Single Product Area =================-->
<div class="product_image_area section_padding">
    <div class="container">
        <div class="row">
            <div class="col-sm-5">
                <div id="slideshow" class="carousel slide" data-ride="carousel">
                    <div class="carousel-inner">
                        <div width="300" height="400" class="carousel-item active">
                            <img style="width: 300;height: 400;" class="d-block w-100"
                                src="/images/arrivel/{{product.IMG}}.png" alt="First slide">
                        </div width="300" height="400">
                        {{#each subIMG}}
                        <div class="carousel-item">
                            <img width="300" height="400" class="d-block w-100"
                                src="/images/arrivel/{{product.IMG}}.png" alt="Second slide">
                        </div>
                        {{/each}}
                    </div>
                    <a class="carousel-control-prev" href="#slideshow" role="button" data-slide="prev">
                        <span class="carousel-control-prev-icon" aria-hidden="true">
                            <i class="ti-angle-left fa-lg" aria-hidden="true" style="color: black;"></i>
                            </i></span>
                        <span class="sr-only">Previous</span>
                    </a>
                    <a class="carousel-control-next" href="#slideshow" role="button" data-slide="next">
                        <span class="carousel-control-next-icon" aria-hidden="true">
                            <i class="ti-angle-right fa-lg" aria-hidden="true" style="color: black;"></i>
                            </i></span>
                        <span class="sr-only">Next</span>
                    </a>
                </div>
            </div>
            <div class="col-sm-5 offset-lg-1">
                <div class="s_product_text">
                    <h2 id="product-name">{{product.Name}}</h2>
                    <h3>Giá hiện tại: {{Numberformat product.Price}} VND</h3>
                    {{#if product.stepPrice}}
                    <h2>Giá mua ngay: {{Numberformat product.InstancePrice}} VND</h2>
                    {{/if}}
                    {{#if InstancePrice}}
                    <h2>Giá mua ngay: {{Numberformat product.InstancePrice}} VND</h2>
                    {{/if}}
                    <ul class="list">
                        <li>
                            <div> Thể loại: <a href="/Category/{{catName.CatName}}/products">{{catName.CatName}}</a>
                            </div>
                        </li>
                        <li>
                            <div>
                                <span class="col-sm-6">Người ra giá: {{auction.Bidder}} </span>
                                <span class="col-sm-6">Score: {{Percentformat bidScore}}</span>
                            </div>
                        </li>
                        <li>
                            <div>
                                <span class="col-sm-6">Người bán: {{auction.Bidder}} </span>
                                <span class="col-sm-6">Score: {{Percentformat bidScore}}</span>
                            </div>
                        </li>
                        <li>
                            <div>Thời gian đăng: {{DateTimeFormat product.StartTime}}</div>
                        </li>
                        <li>
                            <div>Thời gian kết thúc: <span>{{EndTimeFormat product.EndTime}}</span></div>
                        </li>
                    </ul>
                    <p>
                        {{product.Description}}
                    </p>
                    <div class="card_area">
                        <div class="col-sm-12 mt-10">
                            <form method="POST" action="/detail/auction/{{ID}}">
                                <div class="col-lg-6 col-sm-12">
                                    <input type="number" name="Price"
                                        value="{{product.Price}}" min="{{product.Price}}" step="{{product.StepPrice}}">
                                    <input name="StepPrice" value="{{product.StepPrice}}" hidden=true>
                                </div>

                                {{#if invalidPrice}}
                                <div class="text-danger col-sm" id="Invalid-price">Giá không hợp lệ </div>
                                {{/if}}
                                <div class="col-sm-12 mt-10">

                                    <button type="submit" class="btn btn-primary" id="Auction">Đấu giá</button>
                                    <a href="cart.html" class="btn btn-primary m-auto">Mua ngay</a>
                                    <a href="#" class="like_us"> <i class="fa fa-heart fa-lg" style="color:black"
                                            aria-hidden="false"></i> </a>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!--================End Single Product Area =================-->
    <!-- history bidder part -->
    <section class="col-sm-12 mt-30">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-sm-12">
                    <div class="section_tittle text-center">
                        <h2>Lịch sử đấu giá</h2>
                    </div>
                </div>
            </div>
            <table class="table table-striped table-dark table-bordered">
                <thead class="thead-dark">
                    <tr>
                        <th scope="col">STT</th>
                        <th scope="col">Người đấu giá</th>
                        <th scope="col">Giá</th>
                    </tr>
                </thead>
                <tbody id="history-table">
                    {{#each auction}}
                    <tr>
                        <th>{{STT}}</th>
                        <td>{{Bidder}}</td>
                        <td>{{Numberformat Price}} VND</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
    </section>
    <!-- history bidder end       -->
    <section class="product_description_area">
        <div class="container">
            <ul class="nav nav-tabs" id="myTab" role="tablist">
                <li class="nav-item">
                    <a class="nav-link" id="home-tab" data-toggle="tab" href="#home" role="tab" aria-controls="home"
                        aria-selected="true">Mô tả</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" id="profile-tab" data-toggle="tab" href="#profile" role="tab"
                        aria-controls="profile" aria-selected="false">Thông số</a>
                </li>
            </ul>
            <div class="tab-content" id="myTabContent">
                <div class="tab-pane fade" id="home" role="tabpanel" aria-labelledby="home-tab">
                    <p>
                        {{product.Description}}
                    </p>
                </div>
                <div class="tab-pane fade" id="profile" role="tabpanel" aria-labelledby="profile-tab">
                    <div class="table-responsive">
                        <table class="table">
                            <tbody>
                                {{#each properties}}
                                <tr>
                                    <td>
                                        <h5>{{Key}}:</h5>
                                    </td>
                                    <td>
                                        <h5>{{Value}}</h5>
                                    </td>
                                </tr>
                                {{/each}}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </section>
    <section class="product_list best_seller padding_bottom">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-sm-12">
                    <div class="section_tittle text-center">
                        <h2>Sản phẩm tương tự</h2>
                    </div>
                </div>
            </div>
            <div class="row">
                {{#each alikeProduct}}
                <div class="col-lg-4 col-sm-6 mt-10" style="display: bvlock;">
                    <a class="h-100" href="/detail/{{ProductID}}">
                        <div class="card m-auto h-100" style="width: 18rem; background-color: transparent;">
                            <img width="300" height="340" class="card-img-top" src="/images/arrivel/{{IMG}}.png" alt="">
                            <div class="card-body">
                                <h5 class="card-title text-center">{{Name}}</h5>
                                <p class="card-text">
                                    <p>Giá hiện tại: {{Numberformat Price}} VND</p>
                                    {{#if InstancePrice}}
                                    <p>Giá mua ngay: {{Numberformat InstancePrice}} VND</p>
                                    {{/if}}
                                    <p>Ngày kết thúc: {{DateTimeFormat EndTime}}</p>
                                </p>
                            </div>
                    </a>
                    <a class="mt-0 card-footer text-center " href="#">
                        <i class="far fa-heart" style="color: red;"></i>
                        <span class="text-danger"> Yêu thích</span>
                    </a>
                </div>
            </div>
            {{/each}}
        </div>
</section>

{{>footer}}